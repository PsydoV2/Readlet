# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

**Readlet** — a clean, modern e-reader app for iOS/Android. Design inspiration: Notion's minimalism + AirBnB's visual polish. Core principles:

- No account required, no internet needed, no ads
- Local-first: all books stored on device
- Supports EPUB, MOBI (DRM-free), and PDF import
- Reading experience that feels like 2025, not 2010

### Product vision

Users import their own ebooks (EPUB/MOBI/PDF) via the native file picker or share sheet. The app presents a beautiful library view and a distraction-free reading space. No subscriptions, no tracking, no cruft.

### Planned screens / flows

1. **Library** (`app/(auth)/index.tsx`) — grid of imported books with cover art, title, author, reading progress
2. **Import** (`app/(auth)/import.tsx`) — file picker (DocumentPicker) for EPUB/MOBI/PDF; metadata extraction on import
3. **Reader** (`app/(auth)/reader/[id].tsx`) — real EPUB/PDF rendering via a `WebView`, not a native library — see the Reader section below for why and what that trades off
4. **Book detail** (`app/(auth)/book/[id].tsx`) — cover, metadata, reading stats, delete option

All four are **functional**, not a mock-data prototype: importing, storage, and reading are real — see the Library data & import and Reader sections below. There's also a **Settings** modal (`app/(auth)/settings.tsx`, opened from the gear icon in the Library header) covering theme, language, app lock, updates, and legal links — see the Theming, Internationalization, App lock, and OTA updates sections below.

### Sibling `ReadletWeb/` project

`../ReadletWeb` (i.e. `E:\Readlet\ReadletWeb`, a sibling of this directory — the git repo root is `E:\Readlet`, one level up from here) is an unrelated Next.js project, currently just the unmodified `create-next-app` scaffold. It's presumably the future home of the real Datenschutz/Impressum pages `src/constants/LegalLinks.ts` will eventually point at, but nothing there is wired up yet. It has its own `CLAUDE.md`/`AGENTS.md` — don't fold its instructions into this file or treat it as part of the Readlet app itself.

### Key libraries

Already added and in active use — not just planned:

- `expo-document-picker` + `expo-file-system` — file import, copying picked files into app storage
- `expo-sqlite` — local DB for library metadata + reading positions (`src/db/`)
- `jszip` + `fast-xml-parser` — unzip an EPUB and parse its `container.xml`/OPF package document (pure JS, no native module — see Reader section)
- MOBI import (`src/services/mobiService.ts`) needs no library at all — it's a small enough format (PalmDB header + PalmDOC decompression) to parse directly; see the Library data & import section
- `react-native-webview` — renders EPUB/MOBI chapters and PDFs (see Reader section for why, over `react-native-epub-view`/`react-native-pdf`)
- `pdfjs-dist` (devDependency — only its build output is used, copied into `assets/pdfjs/`, never imported as a JS module in RN code) + `expo-asset` — the PDF reader's page-by-page viewer (see Reader section)
- `i18next` + `react-i18next` + `expo-localization` — UI language (see Internationalization section)
- `expo-secure-store` + `expo-local-authentication` — the app-lock feature (see App lock section)
- `expo-updates` — OTA JS-bundle updates for EAS-built binaries (see OTA updates section); inert in Expo Go/dev builds, so it doesn't affect the day-to-day `npx expo start` workflow

### No-auth stance

Auth/session infrastructure from the starter template has been removed — this app is fully local, no backend. `app/AuthScreen.tsx`, `src/context/AuthContext.tsx`, `src/context/UserProvider.tsx`, `src/hooks/useAPI.ts`, `src/hooks/useStorageState.ts`, `src/types/DTOUser.ts`, and `constants/APIRoutes.ts` are gone. `@react-native-async-storage/async-storage` and the `@react-navigation/*` packages were uninstalled with them (expo-router's own `Tabs`/`Stack`/`ThemeProvider` no longer need the raw react-navigation packages as direct dependencies). Web platform support was also dropped (no `react-dom`/`react-native-web`, no `app/+html.tsx`, no `web` script) — this app targets iOS/Android only.

This doesn't contradict the app-lock feature below: `app/(auth)/` and `app/lock.tsx` reuse expo-router's _authentication-rewrites_ pattern name/shape purely for its redirect mechanics (a local device PIN gate, still no account/session/backend), not a resurrection of the removed account auth — hence "app lock"/`AppLockProvider` terminology everywhere instead of "auth".

## Commands

```bash
npm install          # install dependencies
npx expo start        # start dev server (press i/a for iOS/Android)
npm run android        # expo start --android
npm run ios            # expo start --ios
npm test               # jest --watchAll --passWithNoTests (jest-expo preset)
```

```bash
npm run lint          # expo lint (eslint-config-expo)
npx tsc --noEmit       # typecheck (no dedicated script)
```

## Architecture

### Routing (expo-router)

- `app/_layout.tsx` is the root layout. `RootLayout` just nests providers: `SafeAreaProvider > ThemePreferenceProvider > AppLockProvider > ToastProvider > ThemedApp`. `ThemedApp` (a child, so it can read both providers' context) loads fonts, waits on both font-loading and `AppLockProvider`'s `isHydrated` before hiding the splash screen and painting anything — see App lock section below for why — then renders a bare `<Slot />`: it no longer decides lock-vs-app itself, that's `(auth)/_layout.tsx`'s job (see below). As of expo-router 56+, `ThemeProvider`/`DarkTheme`/`DefaultTheme` must be imported from `expo-router` itself, not `@react-navigation/native` — expo-router is no longer compatible with the raw react-navigation package (Metro will fail the bundle otherwise, with `EXPO_ROUTER_DISABLE_RN_NAVIGATION_CHECK=1` as an escape hatch).
- **Auth-style route group for the lock gate**: everything that requires the app to be unlocked lives under `app/(auth)/` (Library, Book detail, Reader, Import, Settings) — mirroring [Expo Router's documented authentication-rewrites pattern](https://docs.expo.dev/router/advanced/authentication-rewrites/). `app/(auth)/_layout.tsx` reads `AppLockProvider`'s `isLocked` and renders `<Redirect href="/lock" />` instead of its `<Stack>` whenever it's true — so the redirect re-evaluates and fires from _any_ screen inside the group the instant the app (re-)locks, not just at initial navigation. `app/lock.tsx` (the actual PIN/biometric screen) lives **outside** the group for exactly that reason — it has to stay reachable while locked — and itself redirects to `/` once `unlock()` flips `isLocked` false. `(auth)` is a pathless group, so none of this changes any URL: `/`, `/book/[id]`, `/import`, `/settings`, etc. are the same paths as if the group didn't exist.
- There is no `unstable_settings.initialRouteName` — the app boots straight into `app/(auth)/index.tsx` (the Library screen) via normal expo-router file-based routing, unless app-lock is enabled, in which case the `(auth)` redirect sends it to `/lock` first.
- **No default expo-router chrome**: `(auth)/_layout.tsx`'s `Stack` sets `screenOptions={{ headerShown: false }}`, and there is no tab bar (no `(tabs)` group) — a single-tab bottom bar added nothing and the native header/page-title look didn't fit the Notion/Airbnb-minimal design goal. Every screen draws its own chrome instead via `src/components/ScreenHeader.tsx` (back chevron or close "×", centered title) or, in the reader, a custom tap-to-toggle top/bottom bar. When adding a new screen under `app/`, follow this pattern rather than relying on a Stack's native header.
- `app/(auth)/import.tsx` and `app/(auth)/settings.tsx` are registered with `presentation: "modal"` via explicit `<Stack.Screen name="..." options={{ presentation: "modal" }} />` entries inside `(auth)/_layout.tsx`'s `<Stack>` — other routes in the group are still auto-discovered from the file tree; you don't need to list every screen, only the ones that need non-default options.
- **Settings and its PIN flow are flat files, not a nested directory** (`app/(auth)/settings.tsx` + `app/(auth)/settings-pin.tsx`, not `settings/index.tsx` + `settings/pin.tsx`) — a directory+`index.tsx` route was tried first and broke navigation ("route not found" opening Settings), because expo-router's typed routes (`.expo/types/router.d.ts`) only ever generate the fully-qualified `"/settings/index"` as a valid `Href` for that shape, and pushing that string doesn't actually resolve at runtime — adding a `_layout.tsx` to the directory didn't change the generated Href either. Flat sibling files sidestep the ambiguity entirely and are what's proven to work (also how `import.tsx`/`+not-found.tsx` are structured). Prefer flat files over a directory+`index.tsx` for any new route unless there's a strong reason.
- Route structure: `app/(auth)/index.tsx` (Library) → `app/(auth)/book/[id].tsx` (Book detail, pushed) → `app/(auth)/reader/[id].tsx` (Reader, pushed); `app/(auth)/import.tsx` (modal, opened from the "+" icon in the Library header); `app/(auth)/settings.tsx` (modal, opened from the gear icon next to it) → `app/(auth)/settings-pin.tsx` (pushed, `?mode=enable|change|disable`). There is deliberately no floating action button — a solid-color circle clashed with the design system's rule that color lives only in accents, never a background/shape fill — so both Library actions are plain icon buttons in the pinned header (`headerIconButton` style, `surfaceHover` fill + `text`-colored icon, same idiom as `ScreenHeader`'s back/close buttons). Datenschutz/Impressum are **not** in-app routes — they're external links opened via `Linking.openURL` (see Theming section below) — so there's no `app/legal/` group; don't reintroduce in-app legal screens without being asked.
- All four screens read from `LibraryProvider` (`src/context/LibraryProvider.tsx`), which wraps `app/_layout.tsx` — never import `src/db/booksRepository.ts` directly from a screen; go through `useLibrary()` so every screen shares one in-memory book list (loaded once from SQLite) instead of each re-querying the DB.

### Library data & import (`src/db/`, `src/services/`, `src/context/LibraryProvider.tsx`)

- **Storage**: `src/db/database.ts` opens (once, cached) a SQLite DB via `expo-sqlite`'s `openDatabaseAsync` and creates a single `books` table on first run. `src/db/booksRepository.ts` is the only place that touches SQL — `getAllBooks`/`getBookById`/`insertBook`/`updateReadingPosition`/`deleteBook`, mapping rows to/from the `Book` type in `src/types/Book.ts` (`spine` is stored as a JSON string column). `LibraryProvider` loads the full list into memory on mount and exposes it plus CRUD via `useLibrary()` — screens never query the repository directly.
- **Import** (`src/services/importBook.ts`, called via `useLibrary().importBook()`): opens `expo-document-picker` restricted to `application/epub+zip`/`application/pdf`/`application/x-mobipocket-ebook`/`application/vnd.amazon.ebook` (the latter two are the closest thing MOBI/AZW has to a standard MIME type, but neither is universally recognized by every OS picker — `formatFromAsset`'s filename-extension regex, matching `.mobi`/`.azw`/`.azw3`, is the real fallback), copies the picked file into `Paths.document/books/<id>.<ext>` via the new class-based `expo-file-system` API (`File`/`Directory`/`Paths` — **not** the legacy `FileSystem.documentDirectory` function API; SDK 57 ships both, this codebase uses the new one) — via `File.copy()` (a native, stream/channel-based copy), **not** `bytes()`+`write()`, which used to load the picked file's *entire* content into one JS `Uint8Array` and reliably OOM'd on Android for a large PDF (real crash, not theoretical — same failure mode `pdfViewerHtml.ts`'s PDF-loading had to avoid too, see Reader section) — then branches by format:
  - **EPUB** (`src/services/epubService.ts`): an EPUB is just a zip file. Unzips it with `jszip`, reads `META-INF/container.xml` to find the OPF package document path, parses that XML with `fast-xml-parser` for `dc:title`/`dc:creator` and the manifest+spine (the chapter reading order) — then extracts **every** file in the archive (not just chapters) to `Paths.document/books/<id>/`, so the reader's `WebView` can resolve chapters' relative image/CSS references from disk. Throws a (German, untranslated — rare/technical failure) `Error` on a malformed EPUB, which `importBook.ts`/the Import screen surfaces as a toast.
  - **MOBI** (`src/services/mobiService.ts`): unlike EPUB, MOBI isn't a zip — it's PalmDB, an old Palm OS document container (a record-offset table, then the records: record 0 is a PalmDOC+MOBI header, the next N records are the (usually PalmDOC/LZ77-compressed) text, everything after `firstImageIndex` is images). Parsed by hand (see the file's own doc comment for the byte layout) since there's no pure-JS RN-friendly MOBI library to reach for. Two things are hard-rejected with a **translated** (genuinely user-facing, not a rare/technical failure) error rather than a hardcoded-German one: DRM-encrypted files (real Kindle Store purchases; only DRM-free/self-published/converted files are supported) and HUFF/CDIC-compressed text (MOBI's less common dictionary-based compression scheme — only PalmDOC/uncompressed text is implemented). MOBI has no chapter list of its own (it's one continuous HTML document) — `spine` is synthesized by splitting on Mobipocket's `<mbp:pagebreak>` markers, falling back to `<h1>`/`<h2>` boundaries, falling back to a single "chapter" (whole book, no in-book navigation — same tradeoff already accepted for PDF below). Inline images are extracted and their `recindex="N"` references rewritten to a real `src=`; the cover (if any) comes from the EXTH `CoverOffset` metadata record. `Book.isReflowableFormat(format)` (`src/types/Book.ts`) is `true` for both `"epub"` and `"mobi"` — screens branch on that instead of repeating `format === "epub" || format === "mobi"`.
  - **PDF**: no parsing beyond `src/services/pdfService.ts`'s `estimatePdfPageCount` — a byte-pattern heuristic (counts `/Type/Page` object markers), **not a real PDF parser**; can mis-count on PDFs with compressed object streams. Returns `null` (chip hidden in Book detail) rather than a wrong number when it can't tell — including for anything over `MAX_HEURISTIC_SCAN_BYTES` (20MB), where it skips reading the file at all rather than risk the same whole-file-into-memory OOM `importBook.ts`'s copy step and `pdfViewerHtml.ts`'s PDF loading both had to be fixed to avoid (see below and the Reader section). Only used to seed the Book Detail chip before the book's first read — the reader itself (see Reader section) replaces this estimate with pdf.js's real page count the first time the PDF is actually opened.
  - All three: a deterministic placeholder cover color from `src/utils/accentColor.ts` (hashes the book id against a fixed palette), used whenever `coverUri` is null — real cover-art extraction only actually runs for EPUB/MOBI (both declare a cover image); PDFs would still need a rendered first-page thumbnail.
- **Deleting** a book (`useLibrary().removeBook`) removes the DB row and best-effort deletes the copied source file + (for EPUB/MOBI) the extracted folder + (for PDF) its generated pdf.js viewer folder (`deleteBookFiles` in `importBook.ts`, `pdfViewerDirForDeletion` in `src/services/pdfViewerHtml.ts`) — failures there are swallowed, since a missing/already-gone file should never block removing the library entry. The *shared* `Paths.document/pdfjs/` library files (see Reader section) aren't book-specific, so they're never deleted here.
- **Progress** is real and page-based for every format — see the Reader section for the full mechanism and the `Book.currentPosition`/`pagePosition`/`progress` doc comments in `src/types/Book.ts` for the exact formulas.

### Reader (`app/(auth)/reader/[id].tsx`)

- Renders via a `WebView` (`react-native-webview`), not a native EPUB/PDF library — a deliberate tradeoff, chosen explicitly over a custom-dev-client approach so the app stays on Expo Go with no `expo prebuild`/EAS build required. Revisit this decision (and reach for `react-native-pdf` + a native EPUB renderer) if/when that tradeoff no longer holds.
- **All three formats page**, not scroll/chapter-jump — "page" is the unit of navigation (bottom-bar prev/next + swipe) and progress for EPUB, MOBI, and PDF alike, via two different mechanisms (necessarily different — a WebView has no single built-in "give me pages" primitive):
  - **EPUB/MOBI** (`isReflowableFormat(book.format)`, `src/types/Book.ts`): loads the current spine chapter's already-extracted XHTML/HTML file straight from disk (`${book.extractedDir}/${book.spine[i]}`, a `file://` URI). `src/services/readerPagination.ts`'s injected script turns it into a horizontally-paged multi-column CSS layout (`column-width: 100vw`, `column-fill: auto`, an ancestor `overflow: hidden`, paging via `transform: translateX`) — the same technique JS EPUB readers like epub.js use, no native pagination library needed. Reading-margin padding lives on an *inner* `#readlet-pages-content` wrapper, not on the `#readlet-pages` element `column-width`/the paging `transform` are set on — padding on the multi-column element itself shrinks its own content-box below `window.innerWidth`, so every rendered column ends up narrower than the `100vw` the paging math assumes; small per page, but it compounds across a chapter (shipped once as exactly that bug: a growing gap on the right that reset at each chapter's first page). The inner wrapper also needs `box-decoration-break: clone`, or its padding only shows up on the very first/last column instead of every one (`readerPagination.ts`'s doc comment has the full reasoning for both). "Chapter" (for MOBI, the synthesized split described in the Library data & import section, not a real spine) is still the WebView's unit of *loading* — one instance per chapter, remounted via `key={sourceUri}` — but no longer the unit of navigation/progress: a swipe/tap can cross a chapter boundary mid-gesture. `Book.pagePosition` (0–1, within the current chapter — see its doc comment) is what's actually persisted and restored, not a raw page number, since chapter pagination depends on font size and screen size and would be meaningless across either changing.
  - **PDF**: no longer handed straight to the WebView as a `file://` URI (the platform's built-in PDF viewer that rendered inline exposes no page-change events to JS). `src/services/pdfViewerHtml.ts` generates a small pdf.js-based viewer instead, using the reusable `PDFViewer` component from `pdfjs-dist`'s `pdf_viewer.mjs` (the same component pdf.js's own viewer app is built on) in `ScrollMode.PAGE` — native WebView scroll-snapping then *is* the page-swipe gesture, no RN-side pan gesture needed there (contrast with EPUB/MOBI, which has to build its own since it isn't scroll-driven). `book.currentPosition` for PDF is directly the 1-based page number (matching pdf.js's own indexing) and `book.pageCount` gets reconciled from pdf.js's real count the first time a PDF is opened, replacing the import-time byte-pattern heuristic (`src/services/pdfService.ts`) that only ever approximated it.
  - Both formats report page changes back to RN via the same `postMessage({ type: "readletPageInfo", pageNumber, pageCount })` shape and expose the same `window.readletGoToPage(n)` for RN-driven navigation — the reader's `onMessage` handler, bottom bar, and progress persistence are format-agnostic as a result. `Book.progress` (0–1) is `(currentPosition + pagePosition) / spine.length` for EPUB/MOBI, `currentPosition / pageCount` for PDF.
  - `WebView` needs `allowFileAccess`/`allowFileAccessFromFileURLs`/`allowUniversalAccessFromFileURLs` (Android) to load local files at all; `allowingReadAccessToURL` on iOS is `book.extractedDir` for EPUB/MOBI (so a chapter can resolve its own relative image/CSS references) but the whole `Paths.document` root for PDF (the generated viewer in `Paths.document/pdfjs-viewers/<id>/`, the shared pdf.js library in `Paths.document/pdfjs/`, and the book's own PDF in `Paths.document/books/` all need to be readable).
  - **pdf.js bundling**: `pdfjs-dist` ships ESM-only from v4 on (no UMD/classic `.js` build) and the npm package doesn't include the full `viewer.html` demo app (only the pdf.js GitHub repo has that) — hence hand-building a minimal viewer around `PDFViewer` instead of dropping one in. The four build files it needs (`pdf.min.mjs`, `pdf.worker.min.mjs`, `pdf_viewer.mjs`, `pdf_viewer.css`) are copied by hand from `node_modules/pdfjs-dist` into `assets/pdfjs/` with a neutral `.pdfjs` extension (`metro.config.js` — new, extends `assetExts` — makes Metro treat them as opaque binary assets instead of trying to parse them as source; `src/types/pdfjs-assets.d.ts` is the matching ambient module declaration). `src/services/pdfViewerAssets.ts` extracts them once (idempotent) into `Paths.document/pdfjs/`, shared across every PDF, so the generated viewer HTML can reference them by absolute `file://` URI. **Known limitations, documented rather than silently wrong**: no cmaps/standard-fonts bundled (thousands of small files, skipped) — PDFs with embedded fonts (the common case) render fine, PDFs relying on non-embedded CJK fonts may show missing glyphs; no text layer (`textLayerMode: DISABLE`) — no text selection/search, only page rendering/navigation.
  - **The PDF itself is handed to pdf.js as its own `file://` URL** (`pdfjsLib.getDocument({ url })`, which fetches it internally), not read into memory in RN and passed as `data` — an earlier version base64-encoded the file and embedded that as a JS string, which (like the `bytes()`-based copy `importBook.ts` used to do, see Library data & import section) loads the *entire* file into RN's JS heap at once and reliably **OOMs on Android for a large PDF** (hit in practice with a real device, not just a theoretical concern). Letting pdf.js fetch the file itself keeps that memory pressure in the WebView's own process instead. The ESM module worker (`pdf.worker.mjs` loaded as `type: "module"`) pdf.js needs is the one remaining thing about this integration that couldn't be verified against a real device/simulator while building it — worth a deliberate on-device check (both platforms) before trusting it fully, not just a `tsc`/`expo-doctor` pass.
- The chrome (top bar with back+title, bottom bar with page progress/nav) is **always visible**, not tap-to-toggle like the old mock-data version — reliably detecting a "tap to toggle" gesture inside a `WebView`'s own content would need an injected touch listener posting back via `onMessage`, judged not worth the complexity for the first functional pass. Revisit if immersive reading is requested.
- Every back/close button uses `goBack(router, fallbackHref)` from `src/utils/goBack.ts` instead of calling `router.back()` directly. A screen can end up with no navigation history to pop — opened via a deep link, or after a dev-server reload resets the stack to just that screen — and a bare `router.back()` then logs `"GO_BACK was not handled by any navigator"` and does nothing; `goBack` checks `router.canGoBack()` first and replaces with the fallback route otherwise.

### Theming preference (`src/context/ThemePreferenceProvider.tsx`)

- The Settings screen's theme picker (System/Hell/Dunkel) needs to override `useColorScheme()` app-wide, not just read it — so `src/components/useColorScheme.ts` no longer re-exports React Native's hook directly. It now reads `ThemePreferenceProvider`'s resolved `colorScheme`, which falls back to the OS scheme when the preference is `"system"`. Every themed surface (`Themed.tsx`, `Toast.tsx`, the reader's status bar) imports `useColorScheme` from `@/src/components/useColorScheme` — **never** `"react-native"` directly, or it'll silently ignore the user's override. `ThemedApp` (see Routing above) reads the resolved scheme to both paint the splash/system-UI background and drive `ThemeProvider`/`StatusBar` — there's no separate "raw OS scheme at boot" path anymore, since the preference is synchronous in-memory state (no async load to race).
- The preference is **not persisted** yet — it resets to `"system"` on app restart. Wire it up to local storage once one exists rather than reaching for `@react-native-async-storage/async-storage`, which was deliberately removed (see No-auth stance above). (Contrast with the language preference below, which _is_ persisted, via `expo-secure-store` — reasonable to bring theme in line with that the next time this file is touched.)
- Datenschutz/Impressum in Settings are external links, not in-app screens: `ActionRow`'s `onPress` calls `Linking.openURL` (from `"react-native"`) with a URL from `src/constants/LegalLinks.ts`, wrapped in `.catch()` to toast if the OS can't open it. `LegalLinks.ts` currently points at placeholder `readlet.app` URLs since there's no real website yet — swap them for the real, hosted pages before shipping; don't fabricate legal page content in-app instead.

### Internationalization (`src/i18n/`)

- **Functional**, not UI-only (it started as UI-only-by-design, then the user asked to wire it up with i18next — see git history/README roadmap). `src/i18n/index.ts` is a side-effect module (`import "@/src/i18n";` at the very top of `app/_layout.tsx`, before any other import) that synchronously initializes `i18next` with `de`/`en` resources bundled from `src/i18n/locales/{de,en}.json` — synchronous because `expo-secure-store` has sync `getItem`/`setItem` alongside the async versions, so the stored language preference can be read _before_ `i18next.init()` runs, with zero async hydration and zero flash of the wrong language (contrast with `AppLockProvider`, which genuinely needs async hydration for hardware detection and gates rendering on it — language doesn't need that machinery at all).
- `getLanguagePreference()`/`setLanguagePreference()` (also in `src/i18n/index.ts`) are the only things that read/write the stored preference (`"system" | "de" | "en"`, key `readlet.languagePreference` in SecureStore) and call `i18next.changeLanguage()`. Settings' language rows call `setLanguagePreference` directly — there's no separate provider/context for this (unlike theme/app-lock) since `react-i18next`'s `useTranslation()` hook already re-renders every consumer automatically when the language changes; a context would just be redundant plumbing.
- Every user-facing string in every screen goes through `useTranslation()`'s `t()` — including toasts and `Error` messages thrown from service code outside a component (via the default `i18next` export imported from `@/src/i18n`, e.g. `i18next.t("import.errors.unsupportedFormat")`): `importBook.ts`'s "unsupported format" case, and `mobiService.ts`'s DRM/unsupported-compression rejections — both common-enough, genuinely user-facing failures to be worth translating, unlike the exception below. The one deliberate exception: `epubService.ts`'s and `mobiService.ts`'s own deep parser-validation errors (malformed `container.xml`/OPF, a corrupt PalmDB/MOBI header) stay hardcoded German — rare, technical, developer-facing-in-practice failures, not worth a translation key. When adding new user-facing text anywhere, add a key to **both** `src/i18n/locales/de.json` and `en.json` rather than hardcoding a string — `de` is `fallbackLng`, so a missing `en` key silently falls back to German rather than erroring, which makes a forgotten translation easy to miss.
- Pluralization uses i18next's `_one`/`_other` key suffixes (e.g. `library.count_one`/`library.count_other`, called as `t("library.count", { count })`) — German and English both only need those two forms.

### App lock (`src/context/AppLockProvider.tsx`)

- Unlike theme/language, this is **fully functional**, not a UI mock — the user explicitly asked for a real lock, not a prototype toggle, since a security feature that looks active but isn't would be actively misleading. Enabling "PIN-Sperre" in Settings requires a real 4-digit PIN persisted via `expo-secure-store` (`src/services/appLockStorage.ts` — see that file for why the PIN is stored as-is rather than hashed); enabling the biometric option additionally requires `expo-local-authentication` hardware + at least one enrolled face/fingerprint (`biometricAvailable`), and itself prompts a biometric check before persisting.
- Enforcement is route-based (see Routing above), not a component swap: `AppLockProvider` wraps the whole app in `app/_layout.tsx` and hydrates asynchronously on mount (SecureStore + hardware detection) — `ThemedApp` holds the splash screen up until `isHydrated`. Once ready, `app/(auth)/_layout.tsx` redirects to `app/lock.tsx` whenever `isLocked` is true; that screen has no back button and calls `unlock()` after a correct PIN (`verifyPin`) or successful `authenticateWithBiometrics()`, which redirects it straight back to `/`. Locks on cold start (if a PIN is set) and again whenever `AppState` transitions to `"background"` (not `"inactive"` — that fires for the biometric system prompt itself, an incoming call banner, etc., and would immediately re-lock a screen that's mid-authentication).
- `app/(auth)/settings-pin.tsx` is the one PIN-entry flow for all three cases (`enable`/`change`/`disable`, via `?mode=`), gated in `disable`/`change` behind re-entering the _current_ PIN first — so picking up an unlocked phone isn't enough to turn the lock off. `src/components/PinKeypad.tsx` (`PinDots` + `PinKeypad`) is shared between it and `app/lock.tsx`.
- `app.json` has two plugin entries added for this: `"expo-secure-store"` (auto-added by `npx expo install`) and `["expo-local-authentication", { "faceIDPermission": "..." }]` (the iOS `NSFaceIDUsageDescription`; without it Face ID silently falls back to device passcode). Both are Expo SDK modules available in Expo Go — no dev-client rebuild needed for this feature.
- Settings → "Jetzt sperren" calls `lockNow()` directly, for testing the lock screen without actually backgrounding the app — this is enough to trigger `(auth)/_layout.tsx`'s redirect from wherever the user currently is.

### OTA updates (`expo-updates`, `src/services/otaUpdates.ts`)

- Unlike the Reader's Expo-Go-only WebView approach (see Reader section), OTA updates are a production-distribution concern, not a dev-workflow one — they don't fight the "stay on Expo Go" tradeoff at all: `expo-updates` is a no-op in Expo Go and in dev builds (`Updates.isEnabled` is `false` there), and only ever activates in an EAS-built binary installed on a device. `npx expo start`/Expo Go keep working exactly as before.
- Config lives entirely in `app.json` (`"runtimeVersion": "1.1.0"` — a plain hand-set string, not `{ "policy": "fingerprint" }`; `"updates": { "url": "https://u.expo.dev/<projectId>" }`, and the `"expo-updates"` plugin entry) and `eas.json` (a `"channel"` per build profile — `development`/`preview`/`production` — so `eas update --channel <name>` reaches the matching installed build).
  - **Why a manual string, not `"fingerprint"`**: `"fingerprint"` (the modern managed-workflow default, and what this project started with) derives the runtime version from a hash of native-relevant inputs — config plugins, SDK version, and, critically, every asset `app.json` references (`icon`, `android.adaptiveIcon.foregroundImage`, the splash image). That bit silently orphaned a real OTA push: a Play Store build shipped, then a later commit only swapped `assets/images/icon.png`/`adaptive-icon.png`/`splash.png` (no native dependency change, "just icons") — but since those files feed the fingerprint hash, the runtime version changed anyway, so the next `eas update` published under a runtime version no installed binary matched, and it silently never reached devices (`expo-updates` reports "no update available", not an error). Confirmed by hand: `npx eas-cli build:list --platform android` showed the live Play Store build's `runtime.version` hash, and `npx @expo/fingerprint fingerprint:generate .` on the working tree produced a different hash — direct proof of the mismatch, not just a theory. A hand-set string trades that footgun for one honest tradeoff: **you must remember to bump it yourself** whenever a change is native-relevant enough to need a fresh store build (new native dependency, config plugin change, icon/splash swap, etc.) — bump `runtimeVersion` and cut a new build+submission together; for a pure-JS change (like a component restyle), leave it alone and `eas update` reaches every device on that version fine. No `ios`/`android` native folders exist in this repo (pure managed/CNG workflow).
- With just that config, updates are **fully automatic and need no app code**: `expo-updates` checks on every cold start, downloads a newer update silently in the background if one exists, and applies it on the *next* cold start — never interrupting a session mid-use.
- `src/services/otaUpdates.ts`'s `checkAndApplyUpdate()` exists only for the **manual** "Nach Updates suchen" row in Settings (mirrors the "Jetzt sperren" pattern — a manual trigger for something that also happens on its own): checks now, and if an update is available, downloads and applies it immediately (`Updates.reloadAsync()`, which restarts the app) rather than waiting for the next cold start. Returns a `"unavailable"` result (surfaced as an info toast, not an error) whenever `Updates.isEnabled` is `false` — Expo Go and dev builds included — the same graceful-degradation shape as `biometricAvailable` in `AppLockProvider` for missing hardware.
- Publishing an update is a manual `eas update --channel <development|preview|production>` (or `--auto` to infer the channel from the current git branch) — this repo doesn't wire up CI for it.

### Toasts (`src/context/ToastProvider.tsx`)

- `ToastProvider` / `useToast()` exposes `showToast(message, type?, duration?)` (`type` is `"success" | "error" | "info"`), rendering an animated overlay via `src/components/Toast.tsx`. No backend/auth dependency — use it for import errors, "book added", etc.

### Design system (`src/components/Themed.tsx` + `src/constants/StyleVariables.ts`)

- **[`DESIGN.md`](DESIGN.md)** (project root) is the portable version of everything below — color/type/spacing tokens, component patterns, voice/tone, and explicit guidance for carrying the same look into `../ReadletWeb` or any other Readlet surface. Read it before styling anything outside this app; keep it in sync if `StyleVariables.ts` changes.
- `src/constants/StyleVariables.ts` is the whole design system: Notion-style neutral minimalism crossed with Airbnb-style polish (one confident accent — Notion's own blue, `#2383E2` in light mode — generous rounded corners, sparing soft elevation). Backgrounds are pure white (light) / pure black (dark) — true neutral gray, zero hue, no tint mixed in — the rule Notion and Airbnb both follow because a tinted "off-white"/"near-black" reads as dirty, not clean. Color lives only in the accent/status tokens, never in a background. (The accent was originally a warm terracotta; switched to blue on request — if you see "terracotta" mentioned elsewhere, e.g. commit history, it's stale.)
  - `light`/`dark` are flat color maps sharing the same keys: background layers `canvas` → `surface` → `surfaceHover`. In light mode `canvas`/`surface` are both pure white — cards separate from the page via `border` only, not a background shift. In dark mode `canvas` is pure black (zero cost per pixel on OLED) with `surface` one small step up (`#131313`) so cards keep a hint of depth without giving up the true-black canvas. Also: text `text`/`textMuted`/`textSubtle`; `border`/`borderMuted`; `overlay` (scrim); `primary`/`primarySoft`/`onPrimary`; `secondary`; status colors `success`/`warning`/`danger`/`info` each with a matching `onSuccess`/`onWarning`/`onDanger`/`onInfo` text color (status colors are lightened in dark mode to pop on the black canvas, so their `on*` flips to dark ink there — same reasoning as `onPrimary`). Add new colors to both palettes with the same key.
  - Everything else is a flat, theme-independent token living as a sibling of `light`/`dark` on the same default export (not nested under a `spacing`/`radius`/etc. sub-object) — prefixed by kind: `br*` (`brSm`/`brMd`/`brLg`/`brXl`/`brRound`) for border radius, `gap*` (`gapXSmall`…`gapXXXLarge`) for spacing, `fontSize*`/`fontWeight*`/`lineHeight*` (`lineHeight*` is absolute px, matching RN's `lineHeight` style prop — not a unitless multiplier) for type, and `shadow*` (`shadowSm`/`shadowMd`/`shadowLg`, iOS shadow\* + Android `elevation`) for elevation. Use these instead of hardcoding numbers in a component's `StyleSheet.create`.
- `src/components/Themed.tsx` exports `Text`, `View`, `Card`, `ScreenContent`, and the `useThemeColor(props, colorName)` hook — drop-in replacements for RN's `Text`/`View` that resolve colors via `useThemeColor` (per-component `light`/`dark` prop override → palette default) based on `useColorScheme()`. All screens use these instead of manually resolving `Colors[scheme]` — reach for `useThemeColor({}, "key")` when you need a raw color value (e.g. for a `Pressable`'s dynamic style function or an icon's `color` prop) rather than re-deriving the palette by hand. Note `View`/`Card` default their background to the palette (`canvas`/`surface`) — for a purely structural wrapper nested inside a `Card` that shouldn't repaint its background, use plain RN `View` instead.

### Path aliases

`@/*` maps to the project root — configured in both `tsconfig.json` (`paths`) and `babel.config.js` (`module-resolver`). Both must stay in sync if the alias changes. `components/` and `constants/` live under `src/` (there is no root-level `components/`/`constants/` anymore), so imports go through `@/src/...`. Example: `import Colors from "@/src/constants/StyleVariables"`, `import { useToast } from "@/src/context/ToastProvider"`.

### TypeScript strictness

`tsconfig.json` enables `strict`, `noUncheckedIndexedAccess`, and `noImplicitOverride` on top of `expo/tsconfig.base`. Code should type-check cleanly under these settings (e.g. array/object index access returns `T | undefined`). No `baseUrl` is set (deprecated as of TypeScript 6.0) — `paths` resolve relative to the tsconfig.json directory automatically.

### app.json / config plugins

Splash screen config lives under the `expo-splash-screen` entry in `plugins` (an array-form plugin with `image`/`resizeMode`/`backgroundColor`, plus a `dark: { backgroundColor }` override), not a top-level `splash` key — that key was removed from the config schema. There is likewise no top-level `newArchEnabled` anymore; the New Architecture is mandatory and always on. Run `npx expo-doctor` after touching `app.json` to catch schema drift like this early.

`app.json` is static JSON, so it can't import `src/constants/StyleVariables.ts` — the splash `backgroundColor`/`dark.backgroundColor` and `android.adaptiveIcon.backgroundColor` are the palette's `light.canvas`/`dark.canvas` hex values copied by hand. If the canvas colors change, update them here too.

`plugins` also has bare-string entries for `expo-sqlite`, `expo-localization`, `expo-updates` (see OTA updates section), and `expo-asset` (see Reader section — the PDF viewer's pdf.js assets) — all auto-added by `npx expo install`, no config needed beyond that — alongside `expo-secure-store` and the array-form `expo-local-authentication` (see App lock section). `expo-document-picker`, `expo-file-system`, and `react-native-webview` don't need plugin entries for how they're used here.

`metro.config.js` (new alongside `babel.config.js`/`eslint.config.js` — this project didn't need one before) extends `assetExts` with a custom `pdfjs` extension so the pdf.js build files under `assets/pdfjs/` bundle as opaque binary assets instead of Metro trying to parse them as JS/CSS source — see the Reader section.
